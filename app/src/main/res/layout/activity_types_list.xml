<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary"
    tools:context=".pokemon_types_list">
    <TextView
        android:visibility="invisible"
        android:id="@+id/error_tv_types"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="ERROR..!! TRY AGAIN or CHECK INTERNET"
        android:fontFamily="@font/odin_rounded"
        android:textSize="20sp"
        android:textColor="@color/bgPoison"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/pokemon_types_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="20sp"
        android:scrollbars="vertical" />

</RelativeLayout>