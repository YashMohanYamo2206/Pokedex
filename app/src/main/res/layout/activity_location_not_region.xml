<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary"
    tools:context=".location_list">
    <TextView
        android:visibility="invisible"
        android:id="@+id/error_tv_location"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="ERROR..!! TRY AGAIN or CHECK INTERNET"
        android:fontFamily="@font/odin_rounded"
        android:textSize="20sp"
        android:textColor="@color/bgPoison"/>

    <EditText
        android:id="@+id/detailsearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="10sp"
        android:layout_marginTop="10sp"
        android:layout_marginRight="10sp"
        android:hint="Search"
        android:textColor="#ffffff"
        android:textColorHint="#999999" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/location_not_region_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginLeft="20sp"
        android:layout_marginTop="60sp"
        android:layout_marginRight="20sp"
        android:layout_marginBottom="20sp"
        android:scrollbars="vertical" />

    <ProgressBar
        android:id="@+id/loading"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true" />
</RelativeLayout>